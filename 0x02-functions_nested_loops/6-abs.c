#include "main.h"

/**
 * _abs - calculates the absolute value
 * of integer
 *
 *@n: integer to be calculated
 *
 * Return: Absolute value
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
