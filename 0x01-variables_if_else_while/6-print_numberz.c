#include <stdio.h>

/**
 * main - Program that prints all single digit number starting from 0
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
