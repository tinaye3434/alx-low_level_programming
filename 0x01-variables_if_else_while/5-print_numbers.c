#include <stdio.h>

/**
 * main - Print all single digits of base 10starting from 10
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
